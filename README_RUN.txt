Run instructions (Windows / PowerShell):

1) Open terminal in:
   C:\Users\Prabhman Singh\Desktop\deep-decors-website

2) Install dependencies:
   npm install

3) Start dev server:
   npm run dev

4) Open browser:
   http://localhost:5173

If the terminal has issues with `cd ... && ...`, run the commands separately (no chained &&).

Want to use the Owner Dashboard (content editor at /admin)? See ADMIN_SETUP.md.

